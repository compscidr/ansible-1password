# Requirements file for molecule tests
# Install with: pip install -r requirements.txt

# Python packages
molecule[docker]>=25.0.0
molecule-plugins[docker]>=23.0.0
ansible-core>=2.15.0
ansible-lint>=25.0.0
docker>=4.3.1

# Collections (install manually with ansible-galaxy if needed)
# community.docker>=3.10.2
# ansible.posix>=1.4.0
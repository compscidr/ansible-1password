# Requirements file for molecule tests
# Install with: pip install -r requirements.txt

# Core testing packages
molecule>=25.0.0
molecule-plugins[docker]>=23.0.0
ansible-core>=2.15.0
ansible-lint>=25.0.0

# Note: Also requires 'ansible-galaxy collection install community.docker'
# for full Docker driver support